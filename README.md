This branch serves as the version of the mca plugin that uses a jQuery hack to remove the Add Media button and replacing it with the old one.

A PHP solution is currently being developed while this serves as a placeholder solution.
